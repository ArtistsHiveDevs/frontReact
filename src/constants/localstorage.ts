export enum LocalStorageVariables {
  CURRENT_LANGUAGE = 'currentLang',
  TOKEN_API_KEY = 'tak',
  // Ver src/common/utils/guards/unloggedServicesGuard.ts para la forma del JSON y el límite de uso.
  GUARD_UNLOGGED_SERVICES = 'gus',
}

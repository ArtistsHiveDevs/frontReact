export enum PATHS {
  HOME = "/home",
  ARTISTS = "/artists",
  MAIN = "/",
  EVENTS = "/events",
  NOT_FOUND = "*",
  SHOWS = "/shows",
  PROFILE = "/profile",
  RIDERS = "/riders",
  SEARCH = "/search",
  BASENAME = "/artistsHive",
}

export enum SUB_PATHS {
  ELEMENT_DETAILS = "details",
  AGENDA = "agenda",
}

export enum URL_PARAMETER_NAMES {
  ELEMENT_ID = "id",
}
